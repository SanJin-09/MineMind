# MineMind TODO

从当前 `v0.1` 到完整需求版本的执行计划。

## 当前状态：v0.8 已完成

- [x] 清理 NeoForge 模板示例方块、物品、创造栏、配置和示例文案。
- [x] 初始化客户端 `/ai` 命令框架。
- [x] 支持 `/ai help/on/off/ask/model/key/status/clear/base`。
- [x] 支持单人客户端 AI 模式聊天拦截。
- [x] 支持 OpenAI-compatible Chat Completions 请求。
- [x] 支持本地 JSON 配置、API Key 保存和尾号隐藏。
- [x] 支持短期上下文、异步请求、错误分类和长回复分段。
- [x] 聊天前缀和正文分色，AI 模式消息前缀保持 `[AI]`，模型回复显示 Provider 名称。
- [x] 设置 API Key 后自动获取并缓存可用文本模型列表。
- [x] 请求开始时显示“正在等待回复......”，并为模型注入 Minecraft 聊天栏场景前置 prompt。
- [x] 内置 OpenAI、DeepSeek、Qwen、KiMi、GLM、Seed、Grok、Gemini Provider。
- [x] 支持玩家主动 `@hotbar`、`@inventory`、`@here`、`@nearby`、`@target` 文本工具。
- [x] 工具结果只附加到本轮请求，不写入短期历史。
- [x] 支持本地 Markdown 长期记忆：`@remember`、`@memory`、`@forget`。
- [x] 支持 `@image` 当前画面截图输入，并在图片请求前提示回复速度会稍慢。
- [x] 模型列表保留视觉对话模型，并为支持图片识别的型号追加 `[图片]` 标记。
- [x] 支持模型在白名单内自主调用本地文本工具和长期记忆工具，`@image` 仍只允许玩家显式触发。

## v0.2：补齐 MVP 稳定性

- [x] 增加 `/ai key list` 或增强 `/ai status`，让已配置服务商状态更清晰。
- [x] 增加 `/ai base` 查看当前服务商 Base URL 的无参形式。
- [x] 增加 `/ai timeout <秒>`，允许玩家调整请求超时。
- [x] 增加 `/ai max-history <数量>`，允许玩家调整上下文最大消息数。
- [x] 校验配置文件字段范围，自动修复非法 timeout、history、base URL。
- [x] 在请求开始时显示等待回复提示，并在完成或失败时结束该状态。
- [x] 确认所有异常路径都会释放请求锁，避免后续请求一直提示处理中。
- [x] 为配置读写、错误分类、上下文裁剪添加单元测试或轻量测试入口。

## v0.3：Provider 抽象完善

- [x] 将 OpenAI-compatible 请求抽象为统一 `AiProvider` 接口。
- [x] 将服务商预设迁移到独立注册表，避免散落在配置类中。
- [x] 每个 Provider 支持默认 display name 和 base URL。
- [x] 支持 Provider 级别 API Key、Base URL、模型 ID 独立保存。
- [x] 增加 Provider 建议补全，命令输入时能补全内置服务商。

## v0.4：主流服务商接入

- [x] DeepSeek：已在 v0.3 按 OpenAI-compatible Provider 接入。
- [x] Qwen / 通义千问：按兼容接口接入，模型只能从服务商 API 返回的可用纯文本模型列表中选择。
- [x] KiMi / Moonshot：按兼容接口接入，模型只能从服务商 API 返回的可用纯文本模型列表中选择。
- [x] GLM / 智谱：按兼容接口接入，模型只能从服务商 API 返回的可用纯文本模型列表中选择。
- [x] Seed / 豆包 / 火山：按兼容接口接入，模型只能从服务商 API 返回的可用纯文本模型列表中选择。
- [x] Grok / xAI：按兼容接口接入，模型只能从服务商 API 返回的可用纯文本模型列表中选择。
- [x] Gemini / Google：实现 Gemini 原生文本对话接口适配。
- [x] 为每个 Provider 验证认证失败、模型不存在、限流、5xx、空回复的错误映射。

## v0.5：Minecraft 文本工具框架

- [x] 设计本地工具白名单协议，例如 `tool.inventory.read`、`tool.location.read`、`tool.entities.nearby`。
- [x] 将可用工具说明写入前置 prompt，要求模型不得假装读取未执行的工具结果。
- [x] 实现快捷栏读取：读取玩家当前 9 格快捷物品栏、物品 ID、数量、显示名和基础组件信息。
- [x] 实现背包读取：读取玩家背包物品、数量、显示名，并控制返回长度避免上下文过长。
- [x] 实现物品标签读取：按需返回物品 tag、附魔、耐久、组件等与提问相关的信息。
- [x] 实现位置读取：获取玩家坐标、维度、朝向、脚下方块和所在生态群系。
- [x] 实现附近生物读取：按半径扫描客户端已加载实体，返回类型、数量、距离和是否敌对。
- [x] 增加玩家主动快捷标记：`@hotbar`、`@inventory`、`@here`、`@nearby`、`@target`。
- [x] 将工具结果作为受控上下文附加到本轮请求，不写入长期历史。
- [x] 为工具输出做长度限制，避免塞爆上下文。

## v0.6：长期记忆

- [x] 在本地 Minecraft 游戏目录维护长期记忆文件，例如 `config/minemind-memory.md`。
- [x] 增加 `@remember <内容>`：调用当前模型整理玩家明确要求记住的内容，再写入长期记忆。
- [x] 增加 `@memory`：读取长期记忆摘要或相关条目并附加到本轮请求。
- [x] 增加记忆工具说明到前置 prompt，让模型知道可以读取长期记忆上下文。
- [x] 写入记忆前做格式化，使用日期、来源和简短条目，避免模型直接覆盖整份文件。
- [x] 支持 `@remember` 与 `@hotbar`、`@inventory`、`@here`、`@nearby`、`@target` 联动整理记忆。
- [x] 提供模型理解后的删除或遗忘机制：`@forget <内容>`。
- [x] 支持 `@forget` 与本地工具联动，由模型选择候选长期记忆条目后后端删除。
- [x] 限制长期记忆读取长度，按关键词匹配并用最近条目补齐。

## v0.7：图片和视觉输入

- [x] 增加 `@image` 快捷标记：截取当前游戏画面并连同文本一起发送。
- [x] 扩展 Provider 能力模型，识别当前模型是否支持图片输入。
- [x] 只有当前模型支持图片输入时才允许 `@image`，否则给出明确错误提示。
- [x] 实现截图质量配置：`low`、`medium`、`high`，控制图片尺寸和请求成本。
- [x] 为 OpenAI、Gemini，以及按 OpenAI-compatible 格式接入的 Qwen、KiMi、GLM、Seed 等视觉对话模型适配图片请求格式。
- [x] 请求前提示玩家截图可能包含聊天、坐标、服务器名等信息。
- [x] 保持默认仍为文本模型选择，图片输入只能由玩家显式 `@image` 触发。

## v0.8：模型自主工具调用

- [x] 设计统一工具调用协议，优先兼容各 Provider 的原生 function/tool calling。
- [x] 为不支持原生工具调用的 Provider 提供 JSON 工具请求 fallback。
- [x] 增加工具调用调度器：解析模型工具请求、校验白名单、执行本地工具、回传工具结果。
- [x] 自主工具白名单包含快捷栏、背包、位置、附近生物、准星目标、长期记忆读取、长期记忆写入、长期记忆删除。
- [x] 截图不注册为自主工具，`@image` 仍只允许玩家显式触发。
- [x] 长期记忆写入和删除按当前策略不二次确认，但在聊天栏提示执行结果。
- [x] 防止工具循环调用，设置单轮最大工具调用次数、最大工具轮次和单轮记忆修改次数。
- [x] 将工具调用过程以聊天消息提示玩家，例如“模型调用工具：背包、位置、长期记忆”。
- [x] 为工具调用增加轻量测试和手动回归清单。

## v0.9：聊天体验和上下文

- [ ] 优化 `/ai help` 排版，按“模式、模型、密钥、状态、上下文”分组显示。
- [ ] 优化 `/ai status`，展示当前模式、服务商、模型、Base URL、Key 状态、上下文条数。
- [ ] 支持 `/ai clear` 后明确显示已删除多少条上下文。
- [ ] AI 模式下保留最近 20 条上下文，且允许配置上限。
- [ ] 每次进入世界默认新会话，不加载长期历史。
- [ ] 玩家输入普通文本时只发送给 AI，不向多人服务器发送。
- [ ] 在多人服务器中禁用 AI 模式，并给出明确提示。
- [ ] 长回复分段时保持前缀一致，避免单条消息过长导致显示难读。

## v0.10：配置和可维护性

- [ ] 整理配置 schema，保留 `currentProvider`、`providers`、`timeoutSeconds`、`maxHistoryMessages`、`streaming`。
- [ ] 支持未来配置迁移版本号，例如 `configVersion`。
- [ ] 添加 README 使用指南：安装、启动、设置 Key、切换模型、常见错误。
- [ ] 添加 Provider 配置示例，说明如何填写兼容接口 Base URL。
- [ ] 添加贡献说明或开发说明，包含 `sh gradlew runClient`、`compileJava`、`build`。
- [ ] 统一中文文案风格，避免命令提示和错误提示口径不一致。

## v1.0：完整需求版验收

- [ ] 单人客户端场景下，玩家可通过原版聊天栏进入 AI 对话模式。
- [ ] 普通聊天在 AI 模式下被客户端拦截，只发送给 AI。
- [ ] AI 回复以 Minecraft 聊天消息显示。
- [ ] 支持 API Key 设置、删除、隐藏显示。
- [ ] 支持 OpenAI、Gemini、Grok、DeepSeek、Qwen、KiMi、Seed、GLM。
- [ ] 模型型号只能从所选服务商 API 返回的可用纯文本模型列表中选择。
- [ ] 支持每个服务商独立 API Key 和 Base URL。
- [ ] 支持明确错误分类：网络、认证、模型、额度、请求、服务、本地。
- [ ] 支持短期上下文、清空上下文、默认新会话。
- [ ] 支持玩家显式长期记忆写入、读取和删除。
- [ ] 不新增实体、方块、物品。
- [ ] 不支持多人服务器统一计费、玩家管理、账号密码登录、语音。
- [ ] 所有核心命令、错误提示和配置行为都有验证记录。
